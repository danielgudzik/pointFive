Run tests and fix all failures.

1. Run `make test`
2. For each failure: read the failing test and the implementation it tests
3. Fix the implementation (don't change tests unless clearly wrong)
4. Re-run `make test` — repeat until all pass