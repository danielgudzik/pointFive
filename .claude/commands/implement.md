Implement the following feature in this Go batch-processing service:

$ARGUMENTS

Process:
1. Read all relevant files fully before writing any code
2. Identify the minimal changes needed — reuse existing types/functions
3. Follow all conventions in CLAUDE.md exactly
4. If pipeline logic changes, add or update a test in `pipeline/pipeline_test.go`
5. Run `make test` — fix any failures before stopping