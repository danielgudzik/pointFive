Review and update all Claude Code config files to reflect the current state of the codebase.

1. Read the current state of key files:
   - `main.go`, `api/server.go`, `api/handlers.go`, `pipeline/pipeline.go`
2. Compare against `CLAUDE.md` — update any stale sections (key files, extension points,
   conventions, commands)
3. Compare against `memory/MEMORY.md` — update any stale patterns or file references
4. Report what was changed and what was already up to date